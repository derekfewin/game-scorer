/**
 * Qwirkle (and other simple scoring games)
 * Uses base class with no modifications
 */

class SimpleScoreGame extends GameBase {
    constructor(config, players, settings) {
        super(config, players, settings);
    }
}